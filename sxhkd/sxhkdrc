#
# wm independent hotkeys
#

# reloads sxhkd configuration:
super + Escape
    pkill -USR1 -x sxhkd

# Launch Terminal
super + Return
    alacritty --config-file /home/tviel/.config/alacritty/alacritty.yml

# Floating Alacritty Terminal
super + shift + Return
    terminal -f

# Fullscreen Alacritty Terminal
super + shift + t
    terminal -m

# j4 dmenu
alt + F1
    rofi -show run

# Launch Menus
super + {x,n,d,e}
    {powermenu,def-nmdmenu,rofi -show run,nemo}

# Lock Screen
super + l
    lock

# custom dmenu scripts
alt + {e,l}
    {edit-configs, quick-links}

# Launch Applications
super + shift + {e,w}
    {nemo,librewolf}

# Volume controls
XF86Audio{RaiseVolume,LowerVolume,Mute}
    volume{ --up, --down, --toggle}

# Brightness controls
XF86MonBrightness{Up,Down}
    brightness{ --up, --down}

# screenshot
Print
    flameshot gui 

